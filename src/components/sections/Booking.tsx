export default function Hero() {
  return (
    <section className="relative bg-center bg-cover bg-no-repeat h-lvh">
      <form>
        <input className="border" placeholder="booknow" />
        <input className="border" placeholder="booknow" />
        <button>Book now</button>
      </form>
    </section>
  );
}
